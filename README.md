# NetflixCommentSection

This is a (first attempt at) a Chrome Extension that allows users to have a comment section for Netflix.

Each comment is time boxed, with a 'start time' and 'end time'.

Stay tuned for more
